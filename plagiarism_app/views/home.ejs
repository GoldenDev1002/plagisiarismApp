<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>Home</title>
</head>
<body>
    <nav id="navigation">
        <div id="navigation_body" >
            <div id="navigation_logo">
                PlagChecker
            </div>
            <div id="navigation_links">
                <ul>
                    <li>Home</li>
                    <li>Nightmode</li>
                    <li>Log In</li>
                </ul>
            </div>
        </div>
    </nav>

  <main >
    <div id="first_background" class="backgrounds">
        <h1 id="title">Plagiarism Checker</h1>
        <p id="text"> Plagcheker detects for plagiarism within your documents within minutes or seconds....</p>

    </div>
    <div id="second_background" class="backgrounds">
       
    </div>
    <form action="/" method="post" id="sendData">
    <textarea id="inputText">
      

        </textarea>
      
 
    <div id="second_background" class="backgrounds">
        <input type="text" name="title" />
      
        <div id="button_containers">
            <input type="file" class="buttons" value="Upload" accept=".pdf, .docx, .doc" name="eachFile" />
            <button class="buttons">Upload </button> <button class="buttons">Scan Documents</button>
        </div>

        <div>
            <% if( documents.length > 0){ %>
               <% documents.forEach((document)=> { %>
                  <h1> <%= document.title %></h1>
                  <p>  <%= document.eachFile %></p>
              <% }) %>


                <% } %>
        </div>
    </div>
  
      </form> 
      
  </main>
  <script>

    let navigationBox = document.querySelector("#navigation_body");
    let unordered_links = document.querySelector("ul");
    let form = document.querySelector("#sendData");
    window.addEventListener("scroll", function(){
        console.log("scrolled");
    let navigationObject = navigationBox.getBoundingClientRect().height;
    console.log(navigationObject)
    let scrollPosition = window.pageYOffset;
    console.log(scrollPosition)

    if(scrollPosition > navigationObject){
      
        console.log("tee")
        navigationBox.classList.add("newNav");
        console.log(navigationBox)
    }else{
        navigationBox.classList.remove("newNav")
    }




    })


    form.addEventListener("click", function(e){
        e.preventDefault();

    })

  </script>
</body>
<footer>

</footer>
</html>